{
  "name": "codbrand-content-builder-validator",
  "version": "1.0.0",
  "private": true,
  "description": "Ground-truth validation of generated block markup using WordPress's own block parser and save() implementations. See references/headless-validation.md.",
  "comment_versions": "These four packages MUST be pinned as a consistent set — mixing versions produces wrong save() output. This set matches the pins used by humanmade/block-runner. Bump them together, deliberately, to match the target WordPress release.",
  "scripts": {
    "validate": "node validate_pattern_wp.cjs"
  },
  "dependencies": {
    "@wordpress/block-editor": "15.23.0",
    "@wordpress/block-library": "10.1.0",
    "@wordpress/blocks": "15.23.0",
    "jsdom": "29.1.1"
  }
}
